# ==============================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# Panel de Monitoreo de Ventas y Actividad de Usuarios - Backend Django
# ==============================================================

# --------------------------------------------------------------
# DATABASE CONFIGURATION
# --------------------------------------------------------------
# Por defecto usa SQLite (no requiere instalación adicional)
# SQLite crea automáticamente el archivo de base de datos
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# ALTERNATIVA: PostgreSQL (requiere instalación y configuración)
# Descomentar y configurar si deseas usar PostgreSQL:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=sales_monitor_db
# DB_USER=postgres
# DB_PASSWORD=your_password_here
# DB_HOST=localhost
# DB_PORT=5432
# Instalar: pip install psycopg2-binary

# ALTERNATIVA: MySQL (requiere instalación y configuración)
# Descomentar y configurar si deseas usar MySQL:
# DB_ENGINE=django.db.backends.mysql
# DB_NAME=sales_monitor_db
# DB_USER=root
# DB_PASSWORD=your_password_here
# DB_HOST=localhost
# DB_PORT=3306
# Instalar: pip install PyMySQL cryptography
# Agregar en sales_monitor/__init__.py:
#   import pymysql
#   pymysql.install_as_MySQLdb()

# --------------------------------------------------------------
# SECURITY SETTINGS
# --------------------------------------------------------------
# Clave secreta de Django - DEBE cambiarse en producción
# Para generar una nueva clave ejecuta:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-in-production

# Modo Debug - SIEMPRE debe ser False en producción
DEBUG=True

# Hosts permitidos - Agregar dominio de producción
# Separar múltiples hosts con comas sin espacios
ALLOWED_HOSTS=localhost,127.0.0.1

# --------------------------------------------------------------
# JWT CONFIGURATION
# --------------------------------------------------------------
# Tiempo de vida de los tokens JWT en MINUTOS

# Access Token: Token de acceso principal (recomendado: 15-60 minutos)
JWT_ACCESS_TOKEN_LIFETIME=60

# Refresh Token: Token para renovar el access token (recomendado: 1-7 días)
JWT_REFRESH_TOKEN_LIFETIME=1440

# --------------------------------------------------------------
# CORS CONFIGURATION
# --------------------------------------------------------------
# URLs del frontend permitidas para hacer peticiones
# Separar múltiples URLs con comas sin espacios
# Ejemplo: http://localhost:3000,https://miapp.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==============================================================
# INSTRUCCIONES DE USO
# ==============================================================
#
# 1. COPIAR ESTE ARCHIVO:
#    Windows: copy .env.example .env
#    Linux/Mac: cp .env.example .env
#
# 2. EDITAR EL ARCHIVO .env:
#    Abre el archivo .env y actualiza los valores según tu entorno
#
# 3. GENERAR SECRET_KEY SEGURA:
#    python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#    Copia el resultado y reemplaza el valor de SECRET_KEY
#
# 4. CONFIGURAR BASE DE DATOS:
#    - SQLite (por defecto): No requiere cambios
#    - PostgreSQL/MySQL: Descomentar y configurar las líneas correspondientes
#
# 5. IMPORTANTE:
#    - NUNCA subas el archivo .env a GitHub (ya está en .gitignore)
#    - El archivo .env contiene información sensible
#    - Cada desarrollador debe tener su propio archivo .env
#
# ==============================================================
# CONFIGURACIÓN PARA PRODUCCIÓN
# ==============================================================
#
# Cuando despliegues en producción, asegúrate de:
#
# 1. Cambiar DEBUG=False
# 2. Generar una nueva SECRET_KEY segura y única
# 3. Configurar ALLOWED_HOSTS con tu dominio
#    Ejemplo: ALLOWED_HOSTS=miapp.com,www.miapp.com
# 4. Usar base de datos robusta (PostgreSQL recomendado)
# 5. Configurar CORS_ALLOWED_ORIGINS con la URL de tu frontend
#    Ejemplo: CORS_ALLOWED_ORIGINS=https://miapp.com
# 6. Considerar usar variables de entorno del servidor
#    (Heroku, Railway, etc. tienen su propio sistema)
#
# ==============================================================